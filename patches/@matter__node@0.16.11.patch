diff --git a/dist/cjs/behaviors/descriptor/DescriptorServer.js b/dist/cjs/behaviors/descriptor/DescriptorServer.js
index a6786485017dd7789ee6a8fc4f0d9033dcdb2ae2..ec1063a331870deffe16fe7741f467e46aa82dec 100644
--- a/dist/cjs/behaviors/descriptor/DescriptorServer.js
+++ b/dist/cjs/behaviors/descriptor/DescriptorServer.js
@@ -46,6 +46,9 @@ class DescriptorServer extends import_DescriptorBehavior.DescriptorBehavior {
     this.reactTo(this.endpoint.lifecycle.changed, this.#updateDescriptor);
     this.state.serverList = this.#serverList;
     this.#initializeDeviceTypeList();
+    if (this.state.endpointUniqueId === void 0) {
+      this.state.endpointUniqueId = `endpoint-${this.endpoint.number}`;
+    }
   }
   /** Initialize device type list when it is not already initialized. */
   #initializeDeviceTypeList() {
@@ -185,8 +188,11 @@ class DescriptorServer extends import_DescriptorBehavior.DescriptorBehavior {
    * Computed current server list.
    */
   get #serverList() {
+    if (this.endpoint.number === 0) {
+      return [29, 40, 48, 62, 31, 51];
+    }
     const list = new Array();
-    for (const type of Object.values(this.endpoint.behaviors.supported)) {
+    for (const type of this.endpoint.behaviors.active) {
       const clusterId = type.cluster?.id;
       if (clusterId) {
         list.push(clusterId);
diff --git a/dist/cjs/endpoints/root.js b/dist/cjs/endpoints/root.js
index aec81c78cc26a8f73d8b87c9be6560d557e5e962..3c39d5a33c94d32dc118af31952ea53d4baa46c2 100644
--- a/dist/cjs/endpoints/root.js
+++ b/dist/cjs/endpoints/root.js
@@ -116,7 +116,8 @@ const RootEndpointDefinition = (0, import_MutableEndpoint.MutableEndpoint)({
     RootRequirements.server.mandatory.GeneralCommissioning,
     RootRequirements.server.mandatory.AdministratorCommissioning,
     RootRequirements.server.mandatory.OperationalCredentials,
-    RootRequirements.server.mandatory.GeneralDiagnostics
+    RootRequirements.server.mandatory.GeneralDiagnostics,
+    RootRequirements.server.optional.NetworkCommissioning
   )
 });
 Object.freeze(RootEndpointDefinition);
diff --git a/dist/cjs/node/server/ServerSubscription.js b/dist/cjs/node/server/ServerSubscription.js
index 9c1aa6236867a1e021828704dcd3150ab99b2240..fbd18b6bd3622d0f65268e9272196994ae682a52 100644
--- a/dist/cjs/node/server/ServerSubscription.js
+++ b/dist/cjs/node/server/ServerSubscription.js
@@ -120,6 +120,7 @@ class ServerSubscription {
   #peerAddress;
   #sendNextUpdateImmediately = false;
   #sendUpdateErrorCounter = 0;
+  #lastErrorWasTimeout = false;
   #currentUpdatePromise;
   constructor(options) {
     const { id, context, request, subscriptionOptions, useAsMaxInterval, useAsSendInterval } = options;
@@ -192,6 +193,7 @@ class ServerSubscription {
     this.#maxInterval = value;
   }
   async handlePeerCancel() {
+    this.#lastErrorWasTimeout = false;
     this.#isCanceledByPeer = true;
     await this.close();
   }
@@ -357,6 +359,7 @@ class ServerSubscription {
             return;
           }
           this.#sendUpdateErrorCounter++;
+          this.#lastErrorWasTimeout = error instanceof import_general.NoResponseTimeoutError || error instanceof import_general.NetworkError || error instanceof import_protocol.SessionClosedError;
           logger.info(
             `Error sending subscription update message (error count=${this.#sendUpdateErrorCounter}):`,
             error instanceof import_general.MatterError && error.message || error
@@ -384,8 +387,8 @@ class ServerSubscription {
             if (error instanceof import_general.NoResponseTimeoutError || error instanceof import_general.NetworkError || error instanceof import_protocol.SessionClosedError) {
               var _stack2 = [];
               try {
+                this.#lastErrorWasTimeout = true;
                 const _messaging = __using(_stack2, updating?.join("canceling"));
-                this.#isCanceledByPeer = true;
                 await this.#cancel();
                 break;
               } catch (_2) {
@@ -680,6 +683,7 @@ class ServerSubscription {
       try {
         if (import_types.StatusResponseError.is(error, import_types.StatusCode.InvalidSubscription, import_types.StatusCode.Failure)) {
           logger.info(`Subscription ${this.idStr} cancelled by peer`);
+          this.#lastErrorWasTimeout = false;
           this.#isCanceledByPeer = true;
         } else {
           import_types.StatusResponseError.accept(error);
diff --git a/dist/esm/behaviors/descriptor/DescriptorServer.js b/dist/esm/behaviors/descriptor/DescriptorServer.js
index 9c40593ff872cce8d40edcd4f01d0399a9461721..20fba4942a3440b860a6ab2f6cdfd0acb3091748 100644
--- a/dist/esm/behaviors/descriptor/DescriptorServer.js
+++ b/dist/esm/behaviors/descriptor/DescriptorServer.js
@@ -23,6 +23,9 @@ class DescriptorServer extends DescriptorBehavior {
     this.reactTo(this.endpoint.lifecycle.changed, this.#updateDescriptor);
     this.state.serverList = this.#serverList;
     this.#initializeDeviceTypeList();
+    if (this.state.endpointUniqueId === void 0) {
+      this.state.endpointUniqueId = `endpoint-${this.endpoint.number}`;
+    }
   }
   /** Initialize device type list when it is not already initialized. */
   #initializeDeviceTypeList() {
@@ -162,8 +165,11 @@ class DescriptorServer extends DescriptorBehavior {
    * Computed current server list.
    */
   get #serverList() {
+    if (this.endpoint.number === 0) {
+      return [29, 40, 48, 62, 31, 51];
+    }
     const list = new Array();
-    for (const type of Object.values(this.endpoint.behaviors.supported)) {
+    for (const type of this.endpoint.behaviors.active) {
       const clusterId = type.cluster?.id;
       if (clusterId) {
         list.push(clusterId);
diff --git a/dist/esm/endpoints/root.js b/dist/esm/endpoints/root.js
index ce17ed4908643f0f06297ec45f578dd8c406e9f7..a6d6d881a81414a03f3c199f214fdd0f73613608 100644
--- a/dist/esm/endpoints/root.js
+++ b/dist/esm/endpoints/root.js
@@ -123,7 +123,8 @@ const RootEndpointDefinition = MutableEndpoint({
     RootRequirements.server.mandatory.GeneralCommissioning,
     RootRequirements.server.mandatory.AdministratorCommissioning,
     RootRequirements.server.mandatory.OperationalCredentials,
-    RootRequirements.server.mandatory.GeneralDiagnostics
+    RootRequirements.server.mandatory.GeneralDiagnostics,
+    RootRequirements.server.optional.NetworkCommissioning
   )
 });
 Object.freeze(RootEndpointDefinition);
diff --git a/dist/esm/node/server/ServerSubscription.js b/dist/esm/node/server/ServerSubscription.js
index 3da9287e7aa8111af3a07e234f0415131eb06304..3b33b278699e4ad0deeef57d2bfd3f84625de2e2 100644
--- a/dist/esm/node/server/ServerSubscription.js
+++ b/dist/esm/node/server/ServerSubscription.js
@@ -123,6 +123,7 @@ class ServerSubscription {
   #peerAddress;
   #sendNextUpdateImmediately = false;
   #sendUpdateErrorCounter = 0;
+  #lastErrorWasTimeout = false;
   #currentUpdatePromise;
   constructor(options) {
     const { id, context, request, subscriptionOptions, useAsMaxInterval, useAsSendInterval } = options;
@@ -195,6 +196,7 @@ class ServerSubscription {
     this.#maxInterval = value;
   }
   async handlePeerCancel() {
+    this.#lastErrorWasTimeout = false;
     this.#isCanceledByPeer = true;
     await this.close();
   }
@@ -360,6 +362,7 @@ class ServerSubscription {
             return;
           }
           this.#sendUpdateErrorCounter++;
+          this.#lastErrorWasTimeout = error instanceof NoResponseTimeoutError || error instanceof NetworkError || error instanceof SessionClosedError;
           logger.info(
             `Error sending subscription update message (error count=${this.#sendUpdateErrorCounter}):`,
             error instanceof MatterError && error.message || error
@@ -387,8 +390,8 @@ class ServerSubscription {
             if (error instanceof NoResponseTimeoutError || error instanceof NetworkError || error instanceof SessionClosedError) {
               var _stack2 = [];
               try {
+                this.#lastErrorWasTimeout = true;
                 const _messaging = __using(_stack2, updating?.join("canceling"));
-                this.#isCanceledByPeer = true;
                 await this.#cancel();
                 break;
               } catch (_2) {
@@ -683,6 +686,7 @@ class ServerSubscription {
       try {
         if (StatusResponseError.is(error, StatusCode.InvalidSubscription, StatusCode.Failure)) {
           logger.info(`Subscription ${this.idStr} cancelled by peer`);
+          this.#lastErrorWasTimeout = false;
           this.#isCanceledByPeer = true;
         } else {
           StatusResponseError.accept(error);
diff --git a/src/behaviors/descriptor/DescriptorServer.ts b/src/behaviors/descriptor/DescriptorServer.ts
index 33cf1714755fc69b0a14cff468bc65a90f9aa9af..72397d5a7cf6dd955ce0d26edddc0958fc31d007 100644
--- a/src/behaviors/descriptor/DescriptorServer.ts
+++ b/src/behaviors/descriptor/DescriptorServer.ts
@@ -40,6 +40,13 @@ export class DescriptorServer extends DescriptorBehavior {
 
         // Initialize DeviceTypeList
         this.#initializeDeviceTypeList();
+
+        // Descriptor attribute 0x0005 (EndpointUniqueId) is optional in spec, but controllers may probe it.
+        // Provide a stable, endpoint-scoped default so reads do not fail with UnsupportedAttribute.
+        const descriptorState = this.state as DescriptorServer.State & { endpointUniqueId?: string };
+        if (descriptorState.endpointUniqueId === undefined) {
+            descriptorState.endpointUniqueId = `endpoint-${this.endpoint.number}`;
+        }
     }
 
     /** Initialize device type list when it is not already initialized. */
@@ -234,8 +241,11 @@ export class DescriptorServer extends DescriptorBehavior {
      * Computed current server list.
      */
     get #serverList() {
+        if (this.endpoint.number === 0) {
+            return [0x001d, 0x0028, 0x0030, 0x003e, 0x001f, 0x0033];
+        }
         const list = new Array<ClusterId>();
-        for (const type of Object.values(this.endpoint.behaviors.supported)) {
+        for (const type of this.endpoint.behaviors.active) {
             const clusterId = (type as { cluster?: { id?: ClusterId } }).cluster?.id;
             if (clusterId) {
                 list.push(clusterId);
diff --git a/src/endpoints/root.ts b/src/endpoints/root.ts
index ebb69192ac499a92f5530ff48ae571d7088ebb72..fabe194a5671cb18d215a4b3aa9cabbe819b87a3 100644
--- a/src/endpoints/root.ts
+++ b/src/endpoints/root.ts
@@ -261,7 +261,8 @@ export const RootEndpointDefinition = MutableEndpoint({
         RootRequirements.server.mandatory.GeneralCommissioning,
         RootRequirements.server.mandatory.AdministratorCommissioning,
         RootRequirements.server.mandatory.OperationalCredentials,
-        RootRequirements.server.mandatory.GeneralDiagnostics
+        RootRequirements.server.mandatory.GeneralDiagnostics,
+        RootRequirements.server.optional.NetworkCommissioning
     )
 });
 
diff --git a/src/node/server/ServerSubscription.ts b/src/node/server/ServerSubscription.ts
index 13953b5a8c9f68e60942e0bebf563eb9ea866d9f..caa1a6502817f4e8f3ac3c998d0b41ee5ac091d0 100644
--- a/src/node/server/ServerSubscription.ts
+++ b/src/node/server/ServerSubscription.ts
@@ -148,6 +148,7 @@ export class ServerSubscription implements Subscription {
 
     #sendNextUpdateImmediately = false;
     #sendUpdateErrorCounter = 0;
+    #lastErrorWasTimeout = false;
     #currentUpdatePromise?: Promise<void>;
 
     constructor(options: {
@@ -242,6 +243,7 @@ export class ServerSubscription implements Subscription {
     }
 
     async handlePeerCancel() {
+        this.#lastErrorWasTimeout = false;
         this.#isCanceledByPeer = true;
         await this.close();
     }
@@ -458,6 +460,10 @@ export class ServerSubscription implements Subscription {
                 }
 
                 this.#sendUpdateErrorCounter++;
+                this.#lastErrorWasTimeout =
+                    error instanceof NoResponseTimeoutError ||
+                    error instanceof NetworkError ||
+                    error instanceof SessionClosedError;
                 logger.info(
                     `Error sending subscription update message (error count=${this.#sendUpdateErrorCounter}):`,
                     (error instanceof MatterError && error.message) || error,
@@ -488,10 +494,10 @@ export class ServerSubscription implements Subscription {
                         error instanceof NetworkError ||
                         error instanceof SessionClosedError
                     ) {
-                        // Let's consider this subscription as dead and wait for a reconnect.  We handle as if the
-                        // controller cancelled
+                        // Treat repeated local send failures as an internal timeout, not as a controller cancel.
+                        // Peer-initiated cancels still set #isCanceledByPeer and trigger operational reannounce.
+                        this.#lastErrorWasTimeout = true;
                         using _messaging = updating?.join("canceling");
-                        this.#isCanceledByPeer = true;
                         await this.#cancel();
                         break;
                     } else {
@@ -787,6 +793,7 @@ export class ServerSubscription implements Subscription {
         } catch (error) {
             if (StatusResponseError.is(error, StatusCode.InvalidSubscription, StatusCode.Failure)) {
                 logger.info(`Subscription ${this.idStr} cancelled by peer`);
+                this.#lastErrorWasTimeout = false;
                 this.#isCanceledByPeer = true;
             } else {
                 StatusResponseError.accept(error);
